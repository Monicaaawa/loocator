import * as React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default function MainContainer(){
    return(
        <View>
            <Text>This file is for Nav Bar stuff. Ignore if not Brooke</Text>
        </View>
    )
}